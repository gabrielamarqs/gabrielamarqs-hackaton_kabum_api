name: deploy-rest-api-hackaton
#
#on:
#  push:
#    branches:
#      - main
#  pull_request:
#    branches:
#      - main
#  workflow_dispatch:
#
#jobs:
#  deployment:
#    runs-on: ubuntu-latest
#    environment: production
#    steps:
#      - name: Deploy to DigitalOcean
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.SSH_HOST }}
#          username: ${{ secrets.SSH_USERNAME }}
#          key: ${{ secrets.SSH_KEY }}
#          port: ${{ secrets.SSH_PORT }}
#          script: |
#            git clone git@github.com:gabrielamarqs/hackaton_kabum_api.git
#            cd /root/hackaton_kabum_api
#            git pull origin main
#            mvn package -Dmaven.test.skip=true
#            java -jar target/HackatonKabum-0.0.1-SNAPSHOT.jar
          # Comandos adicionais de implantação

on: push

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_KEY }}

      - name: Clone Repository and Deploy
        run: |
          git clone git@github.com:gabrielamarqs/hackaton_kabum_api.git
          cd hackaton_kabum_api
          git pull origin main
          mvn package -Dmaven.test.skip=true
